<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.demo.dao.PersonaRepository">

    <insert id="createUser" parameterType="com.example.demo.domain.Persons">
        insert into persons (nickname_user, name, second_name, last_name,
                             second_last_name, mail, password, active, tx_user, tx_host, tx_date)
        values (#{nicknameUser}, #{name}, #{secondName}, #{lastName}, #{secondLastName},
                #{email}, #{password}, 1, #{txUser}, #{txHost}, #{txDate});
    </insert>


</mapper>